<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

    <http pattern="/css/**" security="none"/>
    <http pattern="/images/**" security="none"/>
    <http pattern="/js/**" security="none"/>

  <http auto-config="true" use-expressions="true">
        
        <intercept-url pattern="/map/detail.do" access="hasRole('ROLE_USER')" />          <intercept-url pattern="/api/map/ajaxBookmarkList.do" access="hasRole('ROLE_USER')" /> <intercept-url pattern="/api/bookmark/**" access="hasRole('ROLE_USER')" />        <intercept-url pattern="/review/**" access="hasRole('ROLE_USER')" />              <intercept-url pattern="/map/**" access="permitAll" />       <intercept-url pattern="/api/map/**" access="permitAll" />   <intercept-url pattern="/login/**" access="permitAll" />
        <intercept-url pattern="/home" access="permitAll" />
        <intercept-url pattern="/**" access="permitAll" />

        <form-login 
            login-page="/home" 
            authentication-failure-url="/home?error=true"
            username-parameter="uid"
            password-parameter="pwd" 
        />

        <csrf disabled="true"/>
        
        <logout logout-url="/login/logout.do" 
                logout-success-url="/map/map.do" 
                invalidate-session="true" 
                delete-cookies="JSESSIONID"/>
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider>
             <user-service>
                <user name="user" password="{noop}password" authorities="ROLE_USER"/>
             </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>